machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t pascalgrimaud/ubuntu ubuntu/trusty/
test:
  override:
    - docker run -it pascalgrimaud/ubuntu locale-gen en_US.UTF-8
