machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t pascalgrimaud/test ubuntu/trusty-sub/
test:
  override:
    - docker run -it pascalgrimaud/test locale-gen en_US.UTF-8 | grep up-to-date
